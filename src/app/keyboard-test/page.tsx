// Minimal placeholder to satisfy Next.js route module requirements in production builds.
// If this route is only for local testing, consider removing it entirely before deploy.
export default function KeyboardTestPage() {
	return null;
}

