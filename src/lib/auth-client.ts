"use client"

// BetterAuth client removed. Use Supabase hooks/clients instead.

export const authClient = undefined as never;

export function useSession(): never {
  throw new Error('BetterAuth client has been removed. Use Supabase useSession hook.');
}